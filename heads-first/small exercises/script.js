function sales()

if (inStock == true && (onSale == true || price < 60)) {
    alert("buy buy buy!")
}

function met() {
    var temp = 81
    var willRain = true
    var humid = (temp > 80 && willRain == true)
}

function guesses() {
    var guess = 6
    var isValid = (guess >= 0 && guess <=6)
}

function down() {
    var kB = 1287
    var tooBig = (kB > 1000)
    var urgent = true
    var sendFile = (urgente == true || tooBig == false)
}

function levelUp() {
    var keyPressed = "N"
    var points = 142
    var level
    if (keyPressed == "Y" || (points >100 && points < 200)) {
          level = 2
    } else {
        level = 1
    }
}